# Core (uniquement les dépendances essentielles)
fastapi==0.115.13
pydantic>=2.0.0
uvicorn==0.34.3
python-multipart==0.0.20

# Data processing (uniquement si utilisés par crawl4ai)
pandas>=2.1.4
numpy>=1.26.4
python-frontmatter>=1.1.0
pyyaml>=6.0.1

# Web scraping & APIs (uniquement si utilisés par crawl4ai)
# crawl4ai contraint requests <= 2.32.3, donc on fige à 2.32.3 pour éviter les conflits
requests==2.32.3
beautifulsoup4>=4.12.3
lxml>=4.9.3

# Utils
python-dotenv>=0.21.0
psutil>=7.0.0

# Base de données (uniquement si utilisés)
psycopg2-binary>=2.9.9
supabase>=2.0.0

# Pour crawl4ai et le navigateur
crawl4ai>=0.1.0
playwright==1.54.0
ollama>=0.3.0

# Ne pas forcer --no-binary; on veut les wheels pour playwright/crawl4ai
